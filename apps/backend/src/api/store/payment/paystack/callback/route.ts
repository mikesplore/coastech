// Kept as a backwards-compatible path for callers that can provide a
// publishable API key. Paystack should use the public callback path instead.
export { GET } from "../../../../payment/paystack/callback/route"
