// Backwards-compatible callback path used by the current Paystack dashboard
// configuration. Paystack calls this URL without a publishable API key.
export { GET } from "../../../payment/paystack/callback/route"
